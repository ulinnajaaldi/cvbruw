{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "bun run build",
  "installCommand": "bun install",
  "outputDirectory": "web/dist/client",
  "rewrites": [
    {
      "source": "/((?!assets|fonts|favicon\\.ico|logo|manifest|robots).*)",
      "destination": "/api/server"
    }
  ]
}
